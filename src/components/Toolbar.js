/** 
 * <Toolbar /> – Top Controls Panel

    Possible tools:

        Zoom In / Out (if using a visual tree lib)

        Add Member button

        Search bar to find people

        Export / Print tree
*/
import React from "react";

export default function Toolbar() {
  return <div>Toolbar</div>;
}
