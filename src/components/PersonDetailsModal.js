/**
 * <PersonDetailsModal /> – Expanded Info Dialog

    Purpose: Shows detailed info about a person when selected.

    Behavior:

        Triggered from a PersonNode

        Editable or view-only

        Closes on outside click or “X” button
 */
import React from "react";

export default function PersonDetailsModal() {
  return <div>PersonDetailsModal</div>;
}
