/**
 *  Individual Member Block

    Purpose: Displays a single family member (name, photo, birth date, etc.)
 */
import React from "react";

export default function PersonNode({
  name,
  birthDate,
  photoUrl,
  id,
  children,
  onClick,
}) {
  // const handleClick = () => setSelectedPerson(person);
  return <div>PersonNode</div>;
}
